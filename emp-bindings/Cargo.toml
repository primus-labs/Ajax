[package]
name = "emp-bindings"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
rand = { workspace = true }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

[build-dependencies]
cmake = "0.1"

[features]
verify = []